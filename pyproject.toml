[project]
name = "medford"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.11.7",
]

[project.scripts]
medford = "MEDFORD:parse_args_and_go"